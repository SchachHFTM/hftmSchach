package ch.hftm.Test;

public class TestBoard {

    // private Board testBoard;

    // @BeforeAll
    // public static void init() {
    // // Start the JavaFX runtime
    // Platform.startup(() -> {
    // });
    // }

    // @BeforeEach
    // public void setUp() {
    // // You can do additional setup here if needed
    // }

    // @Test
    // public void testBoardInitialization() {
    // GridPane gridPaneBoard = new GridPane();
    // for (int i = 0; i < 8; i++) {
    // ColumnConstraints columnConstraints = new ColumnConstraints();
    // columnConstraints.setHgrow(javafx.scene.layout.Priority.SOMETIMES);
    // columnConstraints.setMinWidth(10.0);
    // columnConstraints.setPrefWidth(60.0);
    // gridPaneBoard.getColumnConstraints().add(columnConstraints);
    // }

    // // Add row constraints
    // for (int i = 0; i < 8; i++) {
    // RowConstraints rowConstraints = new RowConstraints();
    // rowConstraints.setVgrow(javafx.scene.layout.Priority.SOMETIMES);
    // rowConstraints.setMinHeight(10.0);
    // rowConstraints.setPrefHeight(60.0);
    // gridPaneBoard.getRowConstraints().add(rowConstraints);
    // }

    // testBoard = new Board(gridPaneBoard);

    // assertNotNull(testBoard.squares);
    // assertEquals(64, testBoard.squares.size());

    // // Check if each Square in the board has unique coordinates
    // for (Square square1 : testBoard.squares) {
    // for (Square square2 : testBoard.squares) {
    // if (square1 != square2) {
    // assertNotEquals(square1.x, square2.x);
    // assertNotEquals(square1.y, square2.y);
    // }
    // }
    // }
    // }

    // Add more tests as needed
}